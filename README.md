### Build project
```npm run build-web```

### Start project
```npm run run-build```

### Deploy project to gh-pages
```npm run deploy```

the deploy script should build the project, and create files, index.html and other pages in root folder,
if not just move the files from build folder to root folder and push to gh-pages branch
